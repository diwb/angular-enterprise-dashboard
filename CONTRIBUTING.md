# Contributing

Use small, semantic commits and keep changes covered by tests. Run `npm run lint`, `npm run test:coverage -- --watch=false`, `npm run build`, and the relevant Playwright tests before opening a pull request.
