# Troubleshooting

- If PowerShell blocks `npm.ps1`, use `npm.cmd`.
- If Playwright browsers are missing, run `npx playwright install`.
- If Docker is unavailable locally, rely on the CI Docker build and record the local limitation.
