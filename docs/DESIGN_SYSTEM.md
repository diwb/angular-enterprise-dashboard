# Design System

The design is built from CSS tokens for surface, text, border, state colors, focus, spacing, cards, badges, and responsive layout. Light and dark themes are controlled through `data-theme`.
