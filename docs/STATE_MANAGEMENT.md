# State Management

State uses Angular signals for local feature state: loaded data, loading/error flags, selected records, and derived chart/table values. RxJS is reserved for HTTP or stream-heavy flows when a real API adapter is introduced.
