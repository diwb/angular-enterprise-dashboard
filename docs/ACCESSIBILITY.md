# Accessibility

The UI uses semantic headings, labels, visible focus, keyboard-accessible controls, text alternatives for charts, and a Playwright + axe smoke test. The target is practical WCAG 2.2 AA alignment for implemented flows, not an unsupported claim of absolute conformance.
