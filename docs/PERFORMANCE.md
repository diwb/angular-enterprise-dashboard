# Performance

The app uses lazy-loaded feature pages, production budgets, hashed assets, and encapsulated chart loading inside the dashboard route. The production initial bundle validated locally at approximately 312 kB raw.
